# Requirements for the Tricorder Application

# Core dependencies
pygame==2.6.1
numpy==2.3.3

# Raspberry Pi specific libraries
sense-hat==2.6.0    # For Sense HAT sensor access

# System monitoring
psutil==7.1.0

# 3D Graphics and OpenGL rendering
PyOpenGL==3.1.10
#PyOpenGL-accelerate==3.1.9  # Accelerated OpenGL operations (optional but recommended)
PyGLM==2.8.2        # OpenGL Mathematics library for 3D transformations

# 3D Model support
pygltflib==1.16.5

# Image processing
Pillow==12.1.1

# Media player (audio + video via VLC in separate window)
python-vlc>=3.0.0   # Requires VLC application installed on system (Windows / Raspberry Pi)
mutagen>=1.47.0     # Read MP4 (and MP3) metadata e.g. Comment for display name

# Optional utilities
matplotlib==3.10.6
python-dotenv==1.1.1
